{% extends 'base.html.twig' %} {% block title %}Symfony image parser{% endblock
%} {% block body %}
<style>
    .container-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .container-wrapper code {
        background: #f5f5f5;
        padding: 2px 6px;
    }

    .search-button {
        display: flex;
        padding: 5px;
        background-color: aqua;
        border-radius: 5px;
        margin-left: 10px;
        cursor: pointer;
    }

    .form-control {
        margin-left: 5px;
    }

    #site {
        display: flex;
        flex-direction: row;
    }

</style>

<div class="container-wrapper" style="display: flex; flex-direction: column; align-items: center;">
    <h1>Symfony image parser</h1>

    {{ form_start(form) }}
            {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}